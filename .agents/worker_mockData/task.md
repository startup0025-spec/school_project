# Task: Implement mockData.ts additions

## Objective
Append the offline fallback mock data (`DEFAULT_FALLBACKS`) and the retrieval function (`getFallbackData`) to the end of `C:/Users/user/Desktop/school_contest/Anyway_the_Sea/mobile/constants/mockData.ts`.

## Constraints
1. **Preserve Existing Exports**: Do NOT modify or delete the existing declarations (`NOTIFICATION_HISTORY`, `QUIET_SPOTS`, `WATER_SOURCE_LABELS`, and their corresponding interfaces) in `mockData.ts`. Only append the new code at the end of the file.
2. **Alignment with Architecture**: The appended code must exactly match the design in `C:/Users/user/Desktop/school_contest/Anyway_the_Sea/.agents/orchestrator/handoff.md`.
3. **No Cheating**: All implementations must be genuine. Do not hardcode test results or bypass logic.
