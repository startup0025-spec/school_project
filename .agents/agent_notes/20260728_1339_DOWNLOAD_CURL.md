# Absolute Unified Record Schema

## 1. Context & State
- User requested that the 2 generated APKs be downloaded and placed directly into `C:\Users\user\Desktop\보내는 용도`.
- I have obtained the direct artifact download URLs for both builds from Expo EAS.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will execute a `curl` command to download both `.apk` files into the requested directory.

## 3. Execution Log
- Wrote log.
- Triggering `curl` download commands.
- Yielding response to user.
