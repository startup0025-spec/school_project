# Absolute Unified Record Schema

## 1. Context & State
- User furiously rejected the initial implementation for 3 reasons:
  1. Unwanted literal siren sound (`emergency_siren.wav`) -> Requested intense water mixing instead.
  2. Map deep link not working properly -> Requires OS-agnostic URL schemes.
  3. CDN block / Hardcoding complaint -> The JSON data comes from an upstream source. I need to intercept and sanitize it instead of disabling the CDN fetch.
- User demanded strict codebase tracing before making changes.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]` to write the log immediately.
- I will execute the revised `implementation_plan.md` which includes:
  - Modifying `playDynamicMix` to accept `isDanger` and heavily mix ambient water sounds (no literal sirens).
  - Updating `map.tsx` to use `geo:` and `maps:` URIs.
  - Updating `local_places.ts` to fetch from CDN but intercept and patch the `p-hakjang` coordinates locally.
- Build APKs (`assembleRelease`) after code changes.

## 3. Execution Log
- (Pending) Analyze `audio_engine_service.ts`, `sound.tsx`, `geofencing_service.ts`.
- (Pending) Implement `isDanger` logic in `playDynamicMix`.
- (Pending) Fix deep link in `map.tsx`.
- (Pending) Add sanitizer in `local_places.ts`.
- (Pending) Run `./gradlew assembleRelease` for both Demo and Prod.
