# Absolute Unified Record Schema

## 🍎 BERRY 🍎 LOG
- **Date**: 2026-07-16
- **Task**: Design the mapping pure function resolveOrbMode and propose integration of SpeedClass and Movement.
- **Actions**:
  1. Inspected `mobile/lib/services/geofencing_service.ts` for `TrackingState` and `SpeedClass` definitions.
  2. Designed `resolveOrbMode` pure function mapping `SafetyLevel` and `SpeedClass` / `Movement` to `OrbMode`.
  3. Developed the bridge strategy between background `TrackingState` (via AsyncStorage) and foreground `RippleContext`.
- **Status**: Cycle 2 planning complete, waiting for feedback.
