---
RECORD_ID: "20260716_0930"
RECORD_TYPE: "[MOD]"
TARGET: "C:\\Users\\user\\Desktop\\school_contest\\Anyway_the_Sea\\.agents\\teamwork_preview_explorer_map_cycle6\\"
---
[1_WHAT] (State & Context):
> Cycle 5 Critic 피드백을 수용하기 이전의 `map.tsx` 및 `local_places.ts` 코드는 메모리 누수 위험이 있고, WebView와 React Native 브릿지 간 문자열 이스케이프 구문 에러 가능성이 존재했으며, 속도 제한이 없어 중복 요청이 발생하는 상태였습니다.

[2_HOW] (Action & Details):
> - `local_places.ts`: revalidateData에 FRESHNESS_THRESHOLD = 30000 ms 속도 제한을 적용하고, cache update listener 개수가 15개 초과 시 console.warn 개발자용 경고 처리 추가.
> - `map.tsx`: window.updateSpots가 JSON 문자열이 아닌 자바스크립트 배열 객체를 직접 수신하도록 구조 개선. spotsData의 JSON.stringify 결과를 React Native WebView injectScript에 직접 대입하여 직렬화 시 문자열 따옴표 꼬임 방지.
> - `map.tsx`: WebView 내부에서 마커의 setMap(null) 이전에 clearInstanceListeners()를 호출해 이벤트 해제 및 hasOwnProperty 가드 적용.
> - `map.tsx`: 불필요한 WebGL 유실 리스너 삭제 및 WebView onContentProcessDidTerminate 콜백에 reload() 복구 로직 바인딩.
> - `map.tsx`: getHaversineDistance의 null/undefined/typeof !== 'number'/isNaN 조건 예외 처리 보강 및 getWalkTime 120분 초과 도보시간 대응.

[3_WHY] (Reasoning & Dependency):
> - SWR 패턴의 무차별 재검증 방지로 네트워킹 리소스를 절약하고 불필요한 연산을 방지합니다.
> - WebView 데이터 전달 채널의 직렬화 안전성 강화와 JS SDK 리스너 완전 반환을 보장해 런타임 메모리 누수와 크래시 요인을 최소화합니다.
> - WebGL은 카카오맵에서 사용하지 않으므로 무의미한 코드를 줄이고 모바일 기기에서의 신뢰성을 최대화하기 위함입니다.

[4_NEXT] (Status & Follow-up):
> - [Success]
> - 모든 최종 수정본 코드를 `teamwork_preview_explorer_map_cycle6` 폴더 하위에 안전하게 저장 완료하였으며, TypeScript 빌드가 통과됨을 최종 확인하였습니다.
