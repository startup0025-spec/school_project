---
RECORD_ID: "20260716_0846_MAP_ANALYSIS"
RECORD_TYPE: "[LOG]"
TARGET: "Analyze map UI problem and propose implementation plan"
---
[1_WHAT] (State & Context):
> (LOG: 사장님이 "남은 지도 쪽 코드 뜯어보고 문제 확인해"라고 지시하심.)

[2_HOW] (Action & Details):
> (LOG: 
> 1. `교육청 대회용 앱 간단 설계서.txt` 확인: 구글맵 SDK 대신 정적 일러스트 지도(`quiet-map.png`) 위에 핀을 찍는 아키텍처.
> 2. `mobile/app/(tabs)/map.tsx` 확인: 현재 `mockData.ts`의 `QUIET_SPOTS`를 임포트하여 x, y %비율로 위치를 찍고 있고, 도보 시간(`spot.walk`)도 하드코딩된 상태.
> 3. `core_engine/src/database/local_places.ts` 확인: 실제 DB는 위도(`latitude`)와 경도(`longitude`)를 반환함.
> 4. **구조적 문제 도출**: 
>    - (1) **GPS 투영(Projection)**: 실제 위경도를 정적 일러스트 지도의 픽셀 % 좌표로 변환하기 위해 Bounding Box 수학 공식을 짜서 연결해야 함.
>    - (2) **해안선 우회 거리 오류**: 직선 거리(Haversine) 공식을 쓰면 리아스식 해안의 바다를 가로질러버려 비상식적인 도보 시간이 나올 수 있음.
> 5. 위 분석 결과를 토대로 `implementation_plan.md` 아티팩트를 작성 완료함.
> )

[3_WHY] (Reasoning & Dependency):
> (LOG: 이 문제는 "선만 띡 연결해서 끝날 작업"이 아니라, 좌표계 변환을 위한 유틸리티 함수(`projection.ts`)를 추가하고, 디자이너에게 일러스트 지도의 정확한 GPS Bounding Box 좌표를 받아내야 하는 '기획/아키텍처' 결단이 필요하기 때문에 Implementation Plan을 먼저 작성하여 피드백을 구함.)

[4_NEXT] (Status & Follow-up):
> (LOG: 사장님께 플랜 승인 대기. Bounding Box 좌표 확보 및 길찾기 API 연동 여부 피드백 요청.)
