# Absolute Unified Record Schema

## 1. Context & State
- User explicitly commanded me to add the `map.tsx` bugfix (restoring the refresh button in PROD) to the `implementation_plan.md` so I do not forget it during execution.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will update `implementation_plan.md` to include removing the `!== 'PRODUCTION'` check around the refresh button in `map.tsx`.

## 3. Execution Log
- (Pending) Update `implementation_plan.md` to include `map.tsx` fix.
