# Absolute Unified Record Schema

## 1. Context & State
- User reported that the UI and UX were disconnected from the core features (Audio and GPS) in the previously provided APK.
- The user demanded a thorough investigation without hallucinations and mandated the use of the `agent_notes` directory for logging as per previous protocols.
- The `eas build` process was restored by properly linking the official `react-native-keyboard-controller` and removing local workaround artifacts, ensuring smooth remote building.

## 2. Intent & Rationale
- Trace the signal flow from `geofencing_service.ts` to the UI components to identify the disconnect.
- Discovered that while `geofencing_service.ts` successfully emitted `onTrackingStateUpdate` containing GPS speed (`currentSpeedClass`) and audio `waterType`, `RippleContext.tsx` was only listening for the `isTracking` boolean and entirely ignoring the state payload.
- Fixed `RippleContext.tsx` to actively parse `data.state.currentSpeedClass` into the UI's `movement` state (고요/걷는 중/바쁘게) and parse `data.waterType` into the `waterSource` state (시냇물/강물/바다).
- Triggered the final `eas build` (`task-9631`) to generate the fixed APK for the user.

## 3. Execution Log
- Reverted `package.json` to the official registry module to bypass EAS build errors.
- Corrected imports in `_layout.tsx` back to `react-native-keyboard-controller`.
- Implemented payload mapping in `RippleContext.tsx` to dynamically connect the background GPS/Audio engine to the React UI state.
- Started `npx eas-cli build -p android --profile preview` to extract the final APK as requested.
- Waiting for the EAS build to finish to download the APK.
