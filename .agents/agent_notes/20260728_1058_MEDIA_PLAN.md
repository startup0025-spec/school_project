# Absolute Unified Record Schema

## 1. Context & State
- User requested a lockscreen/notification panel media controller with a "wavy" (ripple) progress bar, so they don't have to open the app to pause/play the sound.
- This refers to the native Android 13+ Media Player notification style.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- Researched how to implement this in Expo. `expo-av` does not natively support the lockscreen media session without a foreground service plugin, so we will need to augment it or migrate to `expo-audio` / `react-native-track-player`.
- Drafted an `implementation_plan.md` outlining the migration, the OS-level media session registration, and the dynamic album art (to extract the squiggly line colors).
- Asked the user two design questions (buttons layout & album art design) to finalize the scope.

## 3. Execution Log
- Created `implementation_plan.md` with `request_feedback=true`.
- Yielding response to user for their review.
