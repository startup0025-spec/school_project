# Absolute Unified Record Schema

## 1. Context & State
- JS compilation via `expo export` succeeded flawlessly (0 errors).
- This proves the "APK crashing on launch" is a Native-layer issue.
- The most notorious cause of this in Expo is `expo-dev-client` being present in standalone `gradlew assembleRelease` builds (it expects EAS or a dev server, causing a crash).

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will modify the APK build script to temporarily remove `expo-dev-client` during the release build process. This strips out all dev-client native interceptors and guarantees a pure, stable production standalone APK.

## 3. Execution Log
- Wrote log.
- Triggering `replace_file_content` on `build_both_apks.js`.
