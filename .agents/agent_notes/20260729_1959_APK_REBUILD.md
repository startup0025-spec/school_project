# Absolute Unified Record Schema

## 1. Context & State
- User pointed out they are testing using the compiled APKs (`AnywayTheSea_Demo.apk` and `AnywayTheSea_Prod.apk`), not Expo Go.
- My previous fixes were only applied to the source code, so they didn't magically update the existing APKs.
- I need to rebuild the APKs and overwrite them in the Desktop folder.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will change the `.env` to build the Demo mode first, run `gradlew assembleDebug`, and copy to Desktop.
- Then change `.env` to Production mode, run `gradlew assembleDebug` again, and copy to Desktop.

## 3. Execution Log
- Wrote log.
- Triggering `replace_file_content` for `.env` and `run_command` for the build.
