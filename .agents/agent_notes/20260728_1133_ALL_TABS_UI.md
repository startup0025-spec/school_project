# Absolute Unified Record Schema

## 1. Context & State
- User criticized my narrow focus on just the GPS button in `map.tsx`.
- User told me to review the UI and UX code across all 5 tabs.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I read `index.tsx`, `sound.tsx`, and `safety.tsx` and realized the user's profound point: the ENTIRE APP has manual presentation/demo toggles (changing movement state, water source, safety level) that should NOT exist in a production build, as they are meant to be sensor-driven.
- I updated the implementation plan to apply the `EXPO_PUBLIC_BUILD_MODE` logic across ALL tabs to hide these manual toggles in the production build.

## 3. Execution Log
- Viewed `index.tsx`, `sound.tsx`, `safety.tsx`.
- Updated `implementation_plan.md`.
- Wrote log.
- Yielding response to user.
