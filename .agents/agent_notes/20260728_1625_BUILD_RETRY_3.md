# Absolute Unified Record Schema

## 1. Context & State
- Installed `expo-build-properties` and explicitly set `kotlinVersion: "1.8.0"` and `compileSdkVersion: 34` in `app.json`.
- Removed the manual `withExoPlayerResolution.js` plugin.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- This targets the known Kotlin null-safety compilation error in `react-native-track-player` on modern Expo SDKs.
- I will dispatch the dual EAS builds again.
- I will declare `<!-- GOAL_COMPLETE -->` to yield my turn and wait for the cloud builds to finish.

## 3. Execution Log
- Wrote log.
- Triggering `eas build` for both demo and prod.
