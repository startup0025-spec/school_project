# Absolute Unified Record Schema

## 1. Context & State
- User approved the native module implementation and initiated a `/goal` to flawlessly integrate Android/iOS lockscreen controls, followed by building the 2 APKs and downloading them to the desktop folder.
- The user emphasized strictness and a 30-year veteran approach ("고인물").

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I am transitioning from Planning to Execution. I will first create a task checklist (`task.md`).
- Since our app mixes 4 overlapping audio tracks concurrently (which `react-native-track-player` cannot do natively), I need to research the best architecture for combining `expo-av` (for mixing) and `react-native-track-player` (for the native lockscreen UI).

## 3. Execution Log
- Wrote log.
- Triggering `search_web` to research the integration architecture.
- Yielding response to user.
