# Absolute Unified Record Schema

## 1. Context & State
- User explicitly commanded me to ONLY focus on the Home tab (`index.tsx`) regarding the new Ripple Bar feature.
- User clarified that their previous instruction ("look at how PROD vs DEMO is split") was meant purely so I knew how to implement the DEMO/PROD logic for the Home tab's Ripple Bar, NOT a global command to fix other tabs.
- I had misunderstood and bloated the scope by adding `safety.tsx` and `sound.tsx` to the implementation plan.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will immediately apologize for overstepping and misinterpreting the scope.
- I will remove the sections about `safety.tsx` and `sound.tsx` from the `implementation_plan.md`.
- I will ensure the plan is strictly focused on the continuous speed ripple for the Home tab.

## 3. Execution Log
- (Pending) Revert the `implementation_plan.md` to exclude other tabs.
- (Pending) Apologize to the user and wait for approval on the corrected plan.
