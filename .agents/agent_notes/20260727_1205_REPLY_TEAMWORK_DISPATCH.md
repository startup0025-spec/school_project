# Absolute Unified Record Schema

## Agent Identity
- **Name:** BERRY 🍎
- **Role:** Agentic Coding Assistant
- **Mode:** /teamwork-preview (Absolute Strictness)

## 1. Context & State
- User reported that the UI/UX in the built APK is disconnected from the core features (Geofencing, KakaoMap, Sound).
- User instructed to deploy the `/teamwork-preview` subagent team to strictly audit the codebase at `C:\Users\user\Desktop\school_contest\Anyway_the_Sea` and trace where the disconnection occurs, without modifying any code.
- User explicitly reminded me to write the chat log according to the rule.

## 2. Action Taken
- Drafted the prompt for `teamwork_preview` and obtained user approval.
- Launched the `teamwork_preview` subagent with the approved prompt to perform a read-only audit.
- Persisted the chat log here before providing the final response to the master, satisfying the Aletheia Pipeline Lock rule.

## 3. Next Steps
- Await the audit report from the `teamwork_preview` team.
- Once the missing links and misconfigurations are identified, formulate a plan to fix them.
