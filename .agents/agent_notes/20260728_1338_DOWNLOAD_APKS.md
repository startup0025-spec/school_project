# Absolute Unified Record Schema

## 1. Context & State
- User requested that the 2 APKs be downloaded directly to `C:\Users\user\Desktop\보내는 용도`.
- I only have the EAS build dashboard links, not the direct `.apk` artifact URLs.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will run `eas-cli build:view <id> --json` for both builds to retrieve the direct artifact URLs.
- Then I will use `curl` to download them to the specified directory.

## 3. Execution Log
- Wrote log.
- Triggering `eas-cli build:view` to extract download URLs.
- Yielding response to user.
