# Absolute Unified Record Schema

## 1. Context & State
- The emulator crash test failed to install the APK because of a signature mismatch (`INSTALL_FAILED_UPDATE_INCOMPATIBLE`).
- `expo prebuild` generates a new `debug.keystore` every time, meaning the previous app must be uninstalled first.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will modify `test_crash.js` to uninstall the package before installing the new one.
- I will run the script again to capture the actual logcat.

## 3. Execution Log
- Wrote log.
- Triggering `replace_file_content` and `run_command`.
