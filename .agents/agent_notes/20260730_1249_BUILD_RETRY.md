# Absolute Unified Record Schema

## 1. Context & State
- The DEMO APK build succeeded, but the PROD APK build failed due to a Windows `EBUSY` file lock on `classes4.dex` during `npx expo prebuild --clean`.
- This is caused by lingering Gradle daemons holding file locks.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will kill the Gradle daemons using `./gradlew --stop` to release the file locks.
- Since the DEMO APK is already built, I will only run `node scripts/build_prod.js` to complete the missing PROD build.

## 3. Execution Log
- (Pending) Run `./gradlew --stop` and `node scripts/build_prod.js`.
