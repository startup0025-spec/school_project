---
RECORD_ID: "20260716_0146"
RECORD_TYPE: "[LOG]"
TARGET: "Review changes required for mockData.ts integration"
---
[1_WHAT] (State & Context):
- The reviewer returned REQUEST_CHANGES due to an integration issue where the URL matching patterns in `mockData.ts` (`/getWaterLevel` and `/getWaterQuality`) do not match the real OpenAPI endpoints used by `busan_api.ts` (`getRvrwtLevelInfo` and `getRiverQualityStation`).
- This prevents the custom Axios response interceptor in `client.ts` from returning mock data during offline launch states.

[2_HOW] (Action & Details):
- We will spawn a Worker subagent to update `getFallbackData` in `mobile/constants/mockData.ts` so that it matches the correct URL patterns for both the mock values and the real OpenAPI endpoints.
- We will also instruct the worker to ensure no typescript type compilation errors remain after this update.
- We will set a safety timer and dispatch.

[3_WHY] (Reasoning & Dependency):
- Absolute Unified Record Schema requires logging.
- Modifying `mockData.ts` to map the real API endpoints correctly is essential to align the mock framework with the newly researched endpoints.

[4_NEXT] (Status & Follow-up):
- Spawn Worker subagent to update mockData.ts and re-verify.
