# Absolute Unified Record Schema

## 1. Context & State
- User corrected my explanation of the `Movement` SegmentedControl on the Home tab (`index.tsx`).
- User stated that `movement` (`calm`, `walking`, `busy`) is designed to be automatically driven by GPS speed.
- The UI control is only for manual override in DEMO mode; in PROD, it must automatically react to the user's actual physical speed.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will investigate `RippleContext.tsx` and `geofencing_service.ts` to trace if/how GPS speed (`SpeedClass`) is wired to `movement` state.
- If the linkage is missing or broken, I will propose a plan to connect `SpeedClass` (from `geofencing_service`) to `setMovement` via `DeviceEventEmitter` or similar mechanism.

## 3. Execution Log
- (Pending) Read `RippleContext.tsx`.
- (Pending) Read `geofencing_service.ts` (check `SpeedClass`).
- (Pending) Propose fix if necessary.
