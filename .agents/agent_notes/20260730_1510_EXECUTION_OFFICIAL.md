# Absolute Unified Record Schema

## 1. Context & State
- User explicitly approved the execution of the TourAPI implementation plan.
- User demanded strict zero-syntax-error implementation, rigorous review, and crash-testing against other parts of the app.
- User went to find alternative solutions for the API outage.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will execute the 3-step plan: `tour_api.ts`, `geofencing_service.ts`, and reducing radii to 1km in mock data.
- I will run `tsc` to prove there are no syntax/type errors.

## 3. Execution Log
- (Pending) Update `task.md`.
- (Pending) Create `tour_api.ts`.
- (Pending) Update `geofencing_service.ts` and mock data.
- (Pending) Run type-check.
