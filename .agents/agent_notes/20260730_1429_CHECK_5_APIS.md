# Absolute Unified Record Schema

## 1. Context & State
- User suspects there are actually 5 APIs used in the app.
- They asked me to verify if all 5 APIs are currently being used in the codebase.
- I previously identified 4 APIs (Busan River Level, Busan River Quality, KMA Forecast, KMA Warnings).

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will search the network layer (`C:\mobile\core_engine\src\network`) to catalog every single API endpoint used.
- I will report back whether we are using 5 APIs and what they are. This does not require a full plan (planning mode bypass).

## 3. Execution Log
- (Pending) Search for `apis.data.go.kr` or other API endpoints.
- (Pending) Report the list of APIs to the user.
