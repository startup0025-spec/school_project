# Absolute Unified Record Schema

## 1. Context & State
- User paused execution to ask two investigatory questions:
  1. What does the "Notification" UI (bell icon) do?
  2. Is the GPS-based fetching/tracking feature already implemented?

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will search the UI codebase (`app/`) to explain the bell icon/notification feature.
- I will confirm that the background GPS geofencing tracker is indeed fully implemented and robust.

## 3. Execution Log
- (Pending) Search for Bell icon in UI.
- (Pending) Answer user.
