# Absolute Unified Record Schema

## 1. Context & State
- User instructed me to remove the "hardcoded stuff" (likely the remaining mock local places in `QUIET_SPOTS`).
- User asked about a 3-letter Github thing we used, struggling to remember the name. I suspect they mean "Gist" (지스트) as a makeshift API for the places.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I will clear the `QUIET_SPOTS` array in `mockData.ts`.
- I will search the codebase for "github" or "gist" to see if we already used one, or I will gently suggest they might be thinking of "Gist" (지스트) or "Raw" (로우) URLs.

## 3. Execution Log
- (Pending) Clear `QUIET_SPOTS`.
- (Pending) Respond to user regarding "Gist".
