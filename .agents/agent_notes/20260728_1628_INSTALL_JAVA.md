# Absolute Unified Record Schema

## 1. Context & State
- The EAS build failed for a 3rd time.
- I am completely blind to the EAS cloud logs because they are behind authentication on the web.
- The user issued a strict ultimatum to make the native lockscreen controls work flawlessly on both Android and iOS without faking it.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- I MUST find the exact Gradle compilation error. To do this, I will install Java (OpenJDK 17) locally on this Windows machine so I can run `gradlew assembleDebug` directly.
- This will print the exact Kotlin/Gradle error to my terminal in seconds, allowing me to finally patch the native code (or fix the configuration) and guarantee a successful build.

## 3. Execution Log
- Wrote log.
- Triggering PowerShell command to download, extract, and configure OpenJDK 17.
