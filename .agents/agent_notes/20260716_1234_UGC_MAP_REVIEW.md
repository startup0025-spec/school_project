---
RECORD_ID: "20260716_1234_UGC_MAP_REVIEW"
RECORD_TYPE: "[LOG]"
TARGET: "Update plan to include UGC review input directly on the Map screen"
---
[1_WHAT] (State & Context):
> (LOG: 사장님께서 다이어리 화면뿐만 아니라 **지도(Map) 화면의 장소 상세 카드에서도 유저가 직접 '나만의 리뷰'를 적을 수 있어야 한다**고 짚어주심.)

[2_HOW] (Action & Details):
> (LOG: 
> 1. `map.tsx`의 하단 카드 UI(`renderCard`) 내부에 유저가 해당 장소에 대한 감상평을 적을 수 있는 `<TextInput>`을 추가함.
> 2. 작성한 리뷰는 장소 ID(`currentPlace.id`)와 매핑하여 `AsyncStorage`에 로컬 저장(`spotReviews` 상태 추가)되도록 `RippleContext.tsx`를 확장함.
> 3. 앱을 껐다 켜도 해당 장소 카드를 열면 내가 썼던 리뷰가 그대로 남아있게 만듦.
> )

[3_WHY] (Reasoning & Dependency):
> (LOG: 장소 탐색(Map)과 기록(Diary)의 맥락을 연결하여, 유저가 특정 장소를 클릭했을 때 바로 감성을 남길 수 있도록 UGC 경험의 완결성을 높이기 위함임.)

[4_NEXT] (Status & Follow-up):
> (LOG: 야매 플랜(implementation_plan.md)에 지도 리뷰 기능을 명시하여 덮어쓰고, 사장님의 최종 컨펌 대기함.)
