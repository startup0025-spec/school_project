# Absolute Unified Record Schema

## 1. Context & State
- Validating the JS bundle using `expo export` to rule out JS syntax errors.
- If JS is valid, the crash is likely caused by `expo-dev-client` interfering with the standalone release build.

## 2. Intent & Rationale
- Comply with `[ALETHEIA PIPELINE LOCK]`.
- Wait for the JS bundle task.
- If successful, the fix will be to temporarily remove `expo-dev-client`, run `prebuild`, and build the Release APK again.

## 3. Execution Log
- Wrote log.
- Yielding turn to wait for background task.
